
  server {
    listen       80;
    access_log   logs/domain1.access.log  main;
    root         html;

    location ^~ /uploads/ {
    root /home/box/web;
  }

  location ~* ^.+\.\w+$ {
    root /home/box/web/public;
  }

  location / {
    return 404;
  }

